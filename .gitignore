node_modules
.next
# Every env file except the committed example. Broad on purpose: a new .env.<something> must be
# ignored by default, not remembered about. `.env.production` was untracked but unignored for a
# while on 2026-07-27 — one `git add -A` from committing a service-role key.
.env
.env.*
!.env.example
.worktrees
.superpowers

# Build artifacts
tsconfig.tsbuildinfo

# Supabase CLI scratch state
supabase/.temp

# Agent worktrees
.claude/worktrees

# Playwright
e2e/.auth
playwright-report
test-results
