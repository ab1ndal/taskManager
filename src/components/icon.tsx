/**
 * Icon sizing tokens. Every glyph in the app comes from `lucide-react` and uses one of these two
 * sizes and this one stroke width — the pre-6.5 codebase had hand-rolled SVGs at six sizes and four
 * stroke widths, which is what made sibling controls look mismatched.
 *
 * PRIMARY is for the leading control of a row or a standalone action; SECONDARY is for supporting
 * controls and anything sitting inside text.
 */
export const ICON_PRIMARY = 20;
export const ICON_SECONDARY = 16;
export const ICON_STROKE = 1.75;
