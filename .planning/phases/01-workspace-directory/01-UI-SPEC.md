---
phase: 1
slug: workspace-directory
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-25
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for workspace directory phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none |
| Icon library | inline SVG |
| Font | Geist Sans (display/UI) + Geist Mono (code) — via `next/font/google` or local |

**Note:** Project uses manual design system with Tailwind v4 CSS custom properties. No shadcn. Pre-existing tokens in `src/app/globals.css` used throughout.

**Font rationale (frontend-design):** Geist Sans is Vercel's typeface — geometric, clean, purpose-built for UI at small sizes. Replaces generic system-sans. Apply via `next/font/local` (Geist is bundled with Next.js 15+). Set as `--font-sans` CSS variable and wire to `font-sans` Tailwind utility in `globals.css`.

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact badge padding, tight element spacing |
| md | 16px | Default element spacing (card padding x-axis) |
| lg | 24px | Section padding, vertical gaps between cards |
| xl | 32px | Layout gaps, section breaks |
| 2xl | 48px | Major section gaps |
| 3xl | 64px | Page-level spacing |

Exceptions: Card vertical padding `py-3` (12px) — this IS on the 4-point grid (12 = 3×4px). Tailwind `py-3` is a standard grid value. No exception to the 4-multiple rule.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 | 1.5 |
| Label / Badge | 11px | 600 | 1.2 |
| Heading 2 (page title) | 20px | 600 | 1.2 |
| Heading 3 (section heading) | 14px | 600 | 1.2 |

**Two weights only:** 400 (body, metadata) and 600 (all headings, labels, badges). Eliminates the mid-weight 500 — badges and headings share the same emphatic weight, creating a cleaner two-level contrast.

**Rationale:** Body at 14px matches existing task-card.tsx (`text-sm`). Heading 2 (20px, 600) from existing workspaces page.tsx (`text-xl font-semibold`). Badge at 11px now 600 for sharper legibility at small size.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #FAF9F7 | Page background, empty surfaces |
| Secondary (30%) | #FFFFFF | Card surfaces, workspace cards |
| Accent (10%) | #7C5CBF | Join buttons, "Joined" badge, interactive states |
| Destructive | #E05252 | Delete confirmations (not in Phase 1 scope) |

**Semantic colors (pre-existing):**
- Text primary: #1C1A24 (for headings, titles)
- Text secondary: #6B6878 (for section labels, metadata)
- Text muted: #A09CB0 (for secondary information, completed tasks)
- Border: #E8E4F0 (card borders, dividers)
- Accent subtle: #EDE8F8 (badge background for accent type)

**Accent reserved for:**
- "Join" button on unjoined workspaces (primary action)
- "Joined" badge visual treatment (success state)
- Loading/pending state opacity transitions
- All other interactive elements must not use accent

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Page heading | "All Workspaces" |
| Primary CTA button | "Create Workspace" |
| Join CTA button | "Join" |
| Joined state badge | "Joined" |
| Empty directory heading | "No workspaces yet" |
| Empty directory body | "Create the first one!" (subtext under heading or beside create button) |
| Kind badge (household) | "Household" |
| Kind badge (work) | "Work" |
| Member count label | "{count} members" |
| Success toast (join) | `Joined "[workspace name]"` |
| Success toast (create) | `Workspace "[name]" created` |
| Error toast (join failed) | `Failed to join "[workspace name]". Please try again.` |
| Error toast (create failed) | `Failed to create workspace. Please try again.` |
| No-workspace banner heading | "You're not in any workspace yet." |
| No-workspace banner CTA | "Browse workspaces" or "Go to workspaces" (link to /workspaces) |

**Notes:**
- All toasts are transient (auto-dismiss after 3-5 seconds)
- No destructive actions in Phase 1 (leaving/deleting workspace deferred to v2)
- Member count: computed server-side at render time

---

## Component Inventory

### Workspace Card (directory entry)
**Container:** `rounded-[8px] border border-[var(--color-border)] bg-[var(--color-surface)] px-4 py-3` with `--shadow-card`
- **Heading:** workspace name, 14px 500, text-primary
- **Kind Badge:** "Household" (indigo-100 bg / indigo-700 text) or "Work" (sky-100 bg / sky-700 text), 11px 500, rounded-full, px-2 py-0.5
- **Member count:** "3 members", 11px, text-muted, right-aligned
- **Action state:**
  - Unjoined: accent-colored "Join" button, 14px 500
  - Joined: green "Joined" badge, 11px 500, matching kind badge styling pattern

### Create Workspace Modal
**Trigger:** "Create Workspace" button in top-right of page header
**Form fields:**
- Workspace name (text input, required)
- Kind selector (radio buttons or dropdown: Household / Work)
- Submit button: "Create"
**Post-success:** Modal closes, toast shows, directory revalidates

### No-Workspace Banner (on /tasks page)
**Trigger:** Show if authenticated user has 0 workspace members
**Content:** "You're not in any workspace yet." + link to /workspaces with text "Browse workspaces"
**Styling:** Top of tasks content area, same card pattern as workspace cards or a distinct alert-style banner

---

## Interaction Patterns

### Join Flow
1. User views workspace card with "Join" button
2. Click "Join" → pending state (button opacity to 0.5 or loading spinner)
3. Server action executes (uses admin client, RLS bypass)
4. On success: button becomes "Joined" badge inline, no page reload
5. Toast: `Joined "[workspace name]"`

### Create Flow
1. Click "Create Workspace" → modal opens
2. User enters name, selects kind, clicks "Create"
3. Pending state on submit button
4. On success: modal closes, directory query revalidates
5. Toast: `Workspace "[name]" created`
6. New workspace appears in list with "Joined" badge

### Error Handling
- Join fails: inline error toast, button returns to "Join" state
- Create fails: inline error toast, modal remains open for retry

---

## Motion & Interaction

| Interaction | Treatment |
|-------------|-----------|
| Join button → pending | `opacity-50 pointer-events-none` transition (100ms) |
| Join button → Joined badge | Cross-fade: button fades out (opacity 0, 150ms), badge fades in (opacity 0→1, 200ms) |
| Modal open | Scale from 0.97→1 + opacity 0→1, 150ms ease-out |
| Modal close | Reverse: scale 1→0.97 + opacity 1→0, 100ms ease-in |
| Card hover | `transition-shadow duration-150` — subtle shadow lift on hover |
| Toast | Slide-in from bottom-right, auto-dismiss after 4s |
| Directory list load | Staggered reveal: each card fades in with 30ms delay increment |

**Implementation:** CSS transitions via Tailwind `transition-*` utilities. No external animation library needed. Stagger via inline `style={{ animationDelay }}` on mapped cards.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | not applicable | not required |
| third-party | none | not required |

No external component registries declared. All UI uses Tailwind utilities and inline SVG icons.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

## Pre-Population Sources

| Source | Decisions Used |
|--------|----------------|
| CONTEXT.md | page structure, directory design, join interaction, post-create experience, no-workspace banner |
| globals.css | color tokens, radius, shadows |
| task-card.tsx | typography scale, badge pattern, spacing pattern |
| workspaces/page.tsx | existing card styling, kind badge colors |
| REQUIREMENTS.md (WS-01 through WS-04) | visual states needed (joined vs unjoined) |

---

*Design contract created: 2026-03-25*
*Phase: 01-workspace-directory*
