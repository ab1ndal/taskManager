---
phase: 04-accessibility-mobile
plan: 06
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05"]
files_modified:
  - .planning/phases/04-accessibility-mobile/04-VERIFICATION.md
autonomous: false
requirements: [U2, U3, U4, U7]
user_setup:
  - service: supabase-auth-dashboard
    why: "Leaked-password protection is disabled in Supabase Auth settings; a dashboard toggle, not code, carried into this phase per STATE.md and CONTEXT.md's 'Manual task for the user'"
    dashboard_config:
      - task: "Enable leaked-password protection"
        location: "Supabase Dashboard → Authentication → Settings → Password protection"

must_haves:
  truths:
    - "Tab order through an open task modal never escapes to the page behind it"
    - "Closing a modal (Escape or Cancel) returns focus to the button that opened it"
    - "A screen reader announces both a success toast and an error toast"
    - "The tasks page has no horizontal overflow or dead space on a real mobile viewport"
    - "Leaked-password protection is enabled in the Supabase Auth dashboard"
  artifacts:
    - path: ".planning/phases/04-accessibility-mobile/04-VERIFICATION.md"
      provides: "Recorded manual pass results for the four jsdom-unreachable checks, per CONTEXT.md D-19"
  key_links: []
---

<objective>
Run the manual verification pass that CONTEXT.md D-19 and RESEARCH.md's Validation Architecture
both call out as unreachable from jsdom — native `<dialog>` top-layer/focus-containment behavior,
screen reader announcement of live regions, and real mobile viewport rendering — and record the
results. Also complete the one non-code manual task carried into this phase: enabling
leaked-password protection in the Supabase Auth dashboard.

Purpose: jsdom's `HTMLDialogElement` implementation is incomplete (jsdom#3294) and cannot exercise
real top-layer stacking or prove focus never escapes a modal; screen reader announcement timing is
an assistive-technology behavior, not a DOM state jest-axe can assert; `100dvh` behavior on a real
mobile browser's collapsing address bar cannot be simulated in a headless test runner. All five
Wave 1/2 plans' automated tests are green by this point — this plan is the human-in-the-loop check
that closes the gap those tests cannot cover, before the phase is considered done.
Output: `.planning/phases/04-accessibility-mobile/04-VERIFICATION.md` recording pass/fail for each
manual check, and the Supabase dashboard toggle enabled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-accessibility-mobile/04-CONTEXT.md
@.planning/phases/04-accessibility-mobile/04-RESEARCH.md
@.planning/phases/04-accessibility-mobile/04-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task: Manual accessibility and mobile verification pass</name>
  <files>.planning/phases/04-accessibility-mobile/04-VERIFICATION.md</files>
  <action>
    Run the four manual checks described in how-to-verify below in a real browser/device, and write
    the results into `.planning/phases/04-accessibility-mobile/04-VERIFICATION.md`. These are the
    checks jsdom cannot prove (native dialog top-layer/focus containment, screen reader
    announcement, real mobile viewport behavior) plus the one non-code dashboard toggle carried into
    this phase (leaked-password protection).
  </action>
  <what-built>
    Every automated Wave 1/2 plan is complete and its test suite is green: the `Dialog` primitive
    (native focus trap/Escape/restore), both task modals converted to `Dialog`, the delete confirm
    dialog with Cancel-first focus, 44px touch targets on every task-card button, task-scoped
    aria-labels, split polite/assertive toast live regions with 44px dismiss buttons, the global
    `:focus-visible` rule, dead nav pills removed, nav hidden when signed out, and `100dvh` on the
    tasks page.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open the app in a real browser (not just the automated suite) to check the
    four things jsdom cannot prove:

    1. **Keyboard walk + focus containment:** Open the new-task modal with the mouse or keyboard.
       Press Tab repeatedly — confirm focus cycles only through elements inside the modal and never
       reaches the page behind it (title bar links, sidebar). Press Shift+Tab from the first field —
       confirm it wraps to the last focusable element inside the modal, not out to the page. Press
       Escape — confirm the modal closes and focus lands back on the "New task" button that opened
       it. Repeat for the edit-task modal (opened via a task card's edit button) and for the delete
       confirm dialog (opened via a task card's delete button) — for the delete dialog, additionally
       confirm the FIRST tab stop on open is the Cancel button, not Delete.

    2. **VoiceOver (macOS Safari) or a mobile screen reader:** Enable the screen reader. Trigger a
       success toast (create a task) and confirm it is announced without interrupting whatever you
       were doing. Trigger an error toast (e.g. submit an edit with an emptied title, or force a
       server error) and confirm it interrupts and announces immediately, and that the toast does
       NOT disappear on its own — you must be able to navigate to its dismiss button and activate it.

    3. **Real mobile device (or Chrome DevTools device emulation with the toolbar showing/hiding):**
       Open the tasks page on an iPhone or Android phone (or emulate one). Scroll the page and rotate
       the address bar to show/hide (scroll down then up in Safari) — confirm the page never shows a
       horizontal scrollbar and there is no dead space at the bottom when the address bar collapses.
       Tap the complete/edit/delete buttons on a task card in quick succession — confirm you can hit
       each one reliably without triggering its neighbor.

    4. **Supabase dashboard:** Log into the Supabase dashboard for this project, navigate to
       Authentication → Settings, and enable "leaked password protection" (the toggle referenced in
       STATE.md's Blockers/Concerns and carried here by CONTEXT.md's "Manual task for the user").

    Record the outcome of all four checks (pass/fail, with any notes) in a new file
    `.planning/phases/04-accessibility-mobile/04-VERIFICATION.md`, using the "Manual-Only
    Verifications" table from 04-VALIDATION.md as the structure (columns: Behavior, Requirement, Why
    Manual, Result).
  </how-to-verify>
  <verify>
    <human-check>04-VERIFICATION.md exists with a recorded pass/fail result for each of the four checks, and the Supabase dashboard toggle is confirmed enabled.</human-check>
  </verify>
  <done>04-VERIFICATION.md is written with all four results recorded, and leaked-password protection is enabled in Supabase.</done>
  <resume-signal>Reply "approved" once all four checks pass and 04-VERIFICATION.md is written, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan performs no code changes beyond writing a verification record file, and toggles a Supabase
Auth security setting (leaked-password protection) that is unambiguously a hardening measure, not a
new trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering (credential stuffing) | Supabase Auth password acceptance | mitigate | Enabling leaked-password protection directly closes this gap — Supabase checks new passwords against known-breach databases before accepting them. |

No other STRIDE categories apply — this plan is verification and a dashboard toggle, not new code.
</threat_model>

<verification>
`.planning/phases/04-accessibility-mobile/04-VERIFICATION.md` exists and records a result for all
four manual checks. The Supabase dashboard shows leaked-password protection enabled.
</verification>

<success_criteria>
- Keyboard focus containment and restoration verified in a real browser for all three dialog callers
- Screen reader announces both toast politeness levels correctly, with error toasts requiring manual dismissal
- Mobile viewport has no overflow/dead-space at 375px with the address bar collapsing/expanding
- Leaked-password protection is enabled in Supabase Auth
- All four Roadmap Phase 4 success criteria (44px targets, focus indicators, dialog semantics, live-region toasts) are confirmed true by a human, not just by automated tests
</success_criteria>

<output>
Create `.planning/phases/04-accessibility-mobile/04-06-SUMMARY.md` when done
</output>
