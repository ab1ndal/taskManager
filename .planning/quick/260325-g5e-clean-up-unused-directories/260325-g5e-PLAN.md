---
phase: quick
plan: 260325-g5e
type: execute
wave: 1
depends_on: []
files_modified:
  - .worktrees/hearth-redesign/
  - .superpowers/brainstorm/
autonomous: true
requirements: [QUICK-CLEANUP]

must_haves:
  truths:
    - "No empty or stale directories under .worktrees/"
    - "No stale brainstorm session directories under .superpowers/"
  artifacts:
    - path: ".worktrees/"
      provides: "Only contains active git worktrees"
    - path: ".superpowers/"
      provides: "Either empty or only contains non-session tooling"
  key_links: []
---

<objective>
Remove stale/empty directories that have accumulated and are no longer in use.

Purpose: Keep the project root clean — unused directories add noise to directory listings and ls outputs.
Output: Two stale directories removed.
</objective>

<execution_context>
@C:/Users/abindal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abindal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Background (from MEMORY.md):
- `.worktrees/` is gitignored — holds git worktrees for feature branches
- `.superpowers/` is gitignored — holds brainstorm session output
- Active worktrees (DO NOT touch): `.worktrees/forgot-password` (branch: feature/forgot-password) and `.claude/worktrees/agent-a8d78525` (branch: worktree-agent-a8d78525)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stale .worktrees/hearth-redesign directory</name>
  <files>.worktrees/hearth-redesign/</files>
  <action>
    The `.worktrees/hearth-redesign/` directory is empty. The `feature/hearth-visual-redesign` branch exists in git but was never registered as a worktree — it has no `.git` file and is not listed by `git worktree list`. It is safe to delete.

    Run: `rm -rf /c/Users/abindal/Documents/00_Python/taskManager/.worktrees/hearth-redesign`

    DO NOT touch `.worktrees/forgot-password` — that is an active registered git worktree.
  </action>
  <verify>
    <automated>ls /c/Users/abindal/Documents/00_Python/taskManager/.worktrees/</automated>
  </verify>
  <done>`.worktrees/` contains only `forgot-password`. `hearth-redesign` is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Remove stale .superpowers/brainstorm session directories</name>
  <files>.superpowers/brainstorm/</files>
  <action>
    The `.superpowers/brainstorm/` directory contains three numeric session subdirectories (`3228-1774371638`, `4271-1774388882`, `5935-1774419653`) — these are stale outputs from prior brainstorm tool runs and serve no ongoing purpose.

    Remove the entire brainstorm directory:
    `rm -rf /c/Users/abindal/Documents/00_Python/taskManager/.superpowers/brainstorm`

    If `.superpowers/` is now empty, remove it too:
    `rmdir /c/Users/abindal/Documents/00_Python/taskManager/.superpowers 2>/dev/null || true`
  </action>
  <verify>
    <automated>ls /c/Users/abindal/Documents/00_Python/taskManager/ | grep -E "superpowers|brainstorm" || echo "clean"</automated>
  </verify>
  <done>`.superpowers/` is gone (or empty with no brainstorm subdir). No stale session directories remain.</done>
</task>

</tasks>

<verification>
After both tasks:

```bash
# Confirm only active worktrees remain
git -C /c/Users/abindal/Documents/00_Python/taskManager worktree list

# Confirm .worktrees only has forgot-password
ls /c/Users/abindal/Documents/00_Python/taskManager/.worktrees/

# Confirm .superpowers is gone
ls /c/Users/abindal/Documents/00_Python/taskManager/ | grep superpowers || echo "clean"
```
</verification>

<success_criteria>
- `.worktrees/hearth-redesign/` deleted
- `.superpowers/brainstorm/` and its session subdirs deleted
- Active git worktrees (forgot-password, agent-a8d78525) untouched and still listed by `git worktree list`
</success_criteria>

<output>
After completion, create `.planning/quick/260325-g5e-clean-up-unused-directories/260325-g5e-SUMMARY.md`
</output>
